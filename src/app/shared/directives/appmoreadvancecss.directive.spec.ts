import { AppmoreadvancecssDirective } from './appmoreadvancecss.directive';

describe('AppmoreadvancecssDirective', () => {
  it('should create an instance', () => {
    const directive = new AppmoreadvancecssDirective();
    expect(directive).toBeTruthy();
  });
});
